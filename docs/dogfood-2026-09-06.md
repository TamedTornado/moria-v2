# Moria restart field report — September 6, 2026

## Product reconciliation before the first V4 TDD run

Observed: retained product documents required cross-GPU qualification while the
TDD rejected it. Discussion also exposed missing multi-view CPU observation
and overly restrictive interpretations of replaceable physics. Historical
20-tick rollback requirements survived although networking is not initial scope.

Impact: generating issues from those documents would reintroduce drift and
unapproved work. This is a planning-input problem, not evidence of a V4 runtime
defect. No new Moria execution has started.

Jason approved a seeded asteroid flythrough with inspection/cutting, early
measured performance, repeatability for testing and deferral of networking
rollback. He directed retiring the TDD and regenerating it through Bro V4 with
adversarial review. The supervisor initially proposed writing the replacement
TDD manually; Jason corrected that. No replacement TDD was written.

Repair: preserve old specifications byte-for-byte in an explicitly historical
directory; replace active seed/GDD/decision record with the reconciled product
baseline. Use new MOR identifiers, without importing old REQ/TECH obligations.
Do not treat the prior TDD's implementation-ready label as proof.

Validation: check archive equality against commit 27e7cc3, active local document
links, requirement/decision references and whitespace. These are document
checks, not a successful FSM/model evaluation or performance qualification.

Next: select drafting and independent adversarial roles with Jason, verify
Viking XP delivery, validate the actual FSM and run it. No deployment, runtime
mutation, decomposition, or model assignment is authorized by this report.
